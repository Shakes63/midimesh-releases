# midi-mesh releases

Public release binaries and auto-update feeds for [midi-mesh](https://github.com/Shakes63/midimesh) (source is private).

Installers and `electron-updater` channel feeds are attached to each release. Grab the latest from the **Releases** tab.
